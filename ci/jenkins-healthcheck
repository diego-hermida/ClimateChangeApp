#!/bin/bash

STATUS_CODE=$(curl -sL -w "%{http_code}" "http://${HOST_IP:-127.0.0.1}:${JENKINS_PORT:-8090}" -o /dev/null --max-time 15)

if [ "$STATUS_CODE" == "200" ]; then
    exit 0
else
    exit 1
fi
